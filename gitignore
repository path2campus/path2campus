# ============================================================
# ENVIRONMENT / SECRETS — tuyệt đối không commit
# ============================================================
.env
.env.local
.env.*.local

# ============================================================
# DEPENDENCIES
# ============================================================
node_modules/
package-lock.json.bak

# ============================================================
# BUILD OUTPUT — sinh ra bởi generate.js, không commit
# Cloudflare Pages tự build lại từ src/ mỗi lần deploy
# ============================================================
dist/

# ============================================================
# OS / EDITOR JUNK
# ============================================================
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# ============================================================
# LOGS
# ============================================================
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# ============================================================
# CSV DATA GỐC (tùy chọn)
# Nếu bạn muốn giữ CSV làm backup/lịch sử data thì XÓA dòng dưới.
# Mặc định bỏ qua vì data đã nằm trong Supabase, không cần trùng lặp trong Git.
# ============================================================
*.csv
